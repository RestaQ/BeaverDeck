apiVersion: v2
name: beaverdeck
description: Helm chart for BeaverDeck, a lightweight Kubernetes operations panel
type: application
version: 2.0.1
appVersion: "1.3.1"
home: https://github.com/arequs/beaverdeck
icon: https://raw.githubusercontent.com/arequs/beaverdeck/main/ui/public/logo.png
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: "Improved pods operations UX with always-visible multi-select, batch actions, and compact row controls."
    - kind: changed
      description: "Pod and workload logs now load older entries on scroll-up and keep scroll position stable."
    - kind: changed
      description: "Cleaned up log rendering and pods table layout issues, including actions-column alignment."
