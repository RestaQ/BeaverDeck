apiVersion: v2
name: beaverdeck
description: Helm chart for BeaverDeck, a lightweight Kubernetes operations panel
type: application
version: 2.0.2
appVersion: "1.3.2"
home: https://github.com/arequs/beaverdeck
icon: https://raw.githubusercontent.com/arequs/beaverdeck/main/ui/public/logo.png
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: "Added pod filtering by name regex and status, with batch selection/actions scoped to the filtered result set."
    - kind: changed
      description: "Reduced navigation and button sizing across the UI for a denser operational layout."
    - kind: changed
      description: "Removed the redundant pod namespace filter from the Pods toolbar in favor of existing namespace preselection."
