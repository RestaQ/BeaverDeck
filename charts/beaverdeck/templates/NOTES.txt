1. Get the application URL by running these commands:
{{- if .Values.ingresses }}
  Check the configured ingress hosts in values.yaml.
{{- else }}
  kubectl -n {{ include "beaverdeck.namespace" . }} port-forward svc/{{ include "beaverdeck.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}